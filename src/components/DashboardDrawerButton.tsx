import { DMSans } from "@/fonts"


export const DashboardDrawerButton = () => {
    return (
        <button className={`${DMSans.className} text-bold text-3xl p-3 m-1 bg-gray-700 hover:bg-gray-800 flex justify-center rounded-xl`}>Test</button>
    )
}